@model Review

<h2>Изтриване на ревю</h2>

<div>
    <p>Сигурни ли сте, че искате да изтриете ревюто на <strong>@Model.User.UserName</strong> за бръснар <strong>@Model.Barber.Name</strong>?</p>
    <p><em>@Model.Content</em></p>
</div>

<form asp-action="DeleteReview">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Да, изтрий</button>
    <a asp-action="Reviews" class="btn btn-secondary">Отказ</a>
</form>
